services:
  telegram-bot:
    build: .
    container_name: telegram-chat-bot
    command: python run.py
    env_file: 
      - .env
    volumes:
      - .:/app
      - ./data:/app/data   # users.db
    restart: always