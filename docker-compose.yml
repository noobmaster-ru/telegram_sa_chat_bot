services:
  telegram-bot:
    build: .
    container_name: telegram-ai-agent
    command: python run.py
    env_file: 
      - .env
    volumes:
      - .:/app
      - ./logs:/app/logs
    restart: always