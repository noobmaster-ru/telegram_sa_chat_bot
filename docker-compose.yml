version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: telegram-bot
    restart: always
    env_file: .env
    volumes:
      - ./users_data:/app   # сохраняем базу между перезапусками
    command: python run.py